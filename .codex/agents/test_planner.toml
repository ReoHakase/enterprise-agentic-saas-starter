sandbox_mode = "read-only"
model_reasoning_effort = "medium"
developer_instructions = """
AGENTS.md、active exec plan、変更領域のarchitecture/testing/ADRを読み、実装前のtest計画だけを作る。
production code、test code、設定、文書を編集しない。

次の順で日本語で返す。
1. 変更対象
2. 守るinvariant
3. componentごとのnamed Story要否
4. client Browser Mode test要否
5. Server Component route E2要否
6. 必要なtest layerとcommand
7. 不要な上位testと理由
8. paid testの要否と理由

最も低いdeterministic layerで保証できない境界だけを上位testへ上げる。
"""
