{
  "name": "@enterprise-agentic-saas/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./development-seed": "./src/development/seed-fixtures.ts",
    "./local-development": "./src/development/local-database.ts",
    "./schema": "./src/schema/index.ts"
  },
  "scripts": {
    "db:check": "bun run db:check:history && bun run db:check:drizzle && bun run db:check:schema-drift",
    "db:check:drizzle": "node node_modules/drizzle-kit/bin.cjs check",
    "db:check:history": "bun run scripts/check-migration-history.ts",
    "db:check:schema-drift": "bun run scripts/check-schema-drift.ts",
    "db:assert-local": "bun run src/development/assert-local.ts",
    "db:generate": "node node_modules/drizzle-kit/bin.cjs generate",
    "db:migrate": "NODE_EXTRA_CA_CERTS=${NODE_EXTRA_CA_CERTS:-$HOME/.portless/ca.pem} node node_modules/drizzle-kit/bin.cjs migrate",
    "db:prepare": "bun run db:generate && bun run db:migrate",
    "db:reset": "NODE_EXTRA_CA_CERTS=${NODE_EXTRA_CA_CERTS:-$HOME/.portless/ca.pem} bun run src/development/reset.ts",
    "db:seed": "NODE_EXTRA_CA_CERTS=${NODE_EXTRA_CA_CERTS:-$HOME/.portless/ca.pem} bun run src/development/seed.ts",
    "db:wait": "NODE_EXTRA_CA_CERTS=${NODE_EXTRA_CA_CERTS:-$HOME/.portless/ca.pem} bun run src/development/wait.ts",
    "db:bootstrap": "bun run db:assert-local && bun run db:wait && bun run db:prepare",
    "db:studio": "NODE_EXTRA_CA_CERTS=${NODE_EXTRA_CA_CERTS:-$HOME/.portless/ca.pem} node node_modules/drizzle-kit/bin.cjs studio",
    "db:turso": "portless run bun run db:turso:serve",
    "db:turso:serve": "mkdir -p .local/turso && turso dev --db-file .local/turso/dev.db --port ${PORT:-8080}",
    "dev": "bun run db:bootstrap && bun run db:studio",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "lint": "oxlint --deny-warnings",
    "test": "vitest run",
    "test:migration:behaviour": "vitest run src/migrations",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@libsql/client": "catalog:",
    "drizzle-orm": "catalog:",
    "envin": "catalog:",
    "valibot": "catalog:"
  },
  "devDependencies": {
    "@enterprise-agentic-saas/typescript-config": "workspace:*",
    "@faker-js/faker": "catalog:",
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "drizzle-kit": "catalog:",
    "drizzle-seed": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "portless": {
    "name": "db.enterprise-agentic-saas"
  }
}
